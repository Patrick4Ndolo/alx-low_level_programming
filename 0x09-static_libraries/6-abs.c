#include "main.h"
/**
  *_abs - computes the absolute value of an integer
  *@n: - it is the integer value in question
  *Return: abs of an integer
  */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}

